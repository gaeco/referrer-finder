---
description: 
globs: 
alwaysApply: true
---
## Project Rules  

- **Introduction**  
  - This project implements an utility
    - extract the function list which is changed between 2 git commits.
    - the changed function should be classified by DELETED, CHANGED, ADDED

- **Build Platform**: Maven
  - build command : mvn

### Rules for the `referrer-finder` Module  
- **Java Source Path**: `./referrer-finder/src/main/java`   
- **Java Test Source Path**: `./referrer-finder/src/test/java` 

#### **Java Package Structure**  
- **Base Package**: `net.gaeco.referrerfinder`

#### **Java Package Details** 
```
referrer-finder/src/main/java
      └──  /net/gaeco/referrerfinder # Finding changed functions from "/com/example/myapp"
      └── /com/example/myapp # compared classes
        ├── controller/                    # Spring @Controller
        ├── service/                    # Spring @Service
        └── vo/              # Value Objects
```
